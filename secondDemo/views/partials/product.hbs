<article class="product">
    <img src="{{imgUrl}}" alt="{{name}} image">
    <h2>{{name}}</h2>
    <p>{{description}}</p>
    <p>${{price}}</p>
    <br>
    <a href="/product/{{id}}" class="btn">Details</a>
</article>